apiVersion: v1
kind: Service
metadata:
  name: crypto-exchange-backend-service
  namespace: crypto-exchange
  labels:
    app: crypto-exchange-backend
spec:
  selector:
    app: crypto-exchange-backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3001
  type: ClusterIP
