apiVersion: v1
kind: Service
metadata:
  name: crypto-exchange-frontend-service
  namespace: crypto-exchange
  labels:
    app: crypto-exchange-frontend
spec:
  selector:
    app: crypto-exchange-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP
